extends layout


block content
    h1.text-info #{title}
    .row
        .col-12
            form(action="/battery/form", method="post")
                .input-group
                    label(for="level")
                    input.form-control(type="text" name="level" placeholder="Nivel Batería")
                .input-group
                    input.btn.btn-success(type="submit" value="Agregar")